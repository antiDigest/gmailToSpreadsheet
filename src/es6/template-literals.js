const userName = 'AntiDigest';

Logger.log(`${userName} has ${userName.length} characters`);
Logger.log(`${userName} in uppercase is  ${userName.toUpperCase()}`);
Logger.log(`The date is ${new Date().toUTCString()}`);
